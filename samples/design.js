const container=document.querySelector('[data-compare]');const buttons=document.querySelectorAll('[data-view]');const panels=document.querySelectorAll('[data-panel]');buttons.forEach(button=>button.addEventListener('click',()=>{const view=button.dataset.view;buttons.forEach(b=>b.setAttribute('aria-pressed',String(b===button)));panels.forEach(panel=>{panel.hidden=view!=='both'&&panel.dataset.panel!==view;});container.classList.toggle('single',view!=='both');}));